<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="200dp"
    android:background="@drawable/dialog_background"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/txt_dia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Ønsker du og starte fra beynnelsen av,\n eller fortsette der du sluttet sist?"
        android:textColor="#5DBCD2"
        android:textSize="16dp"
        android:textStyle="bold"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true">
    </TextView>

    <Button
        android:id="@+id/btn_beginning"
        android:layout_width="100dp"
        android:layout_height="30dp"
        android:background="@android:drawable/btn_default"
        android:clickable="true"
        android:text="Begynnelsen"
        android:textColor="#5DBCD2"
        android:textSize="12dp"
        android:layout_below="@+id/txt_dia"
        android:layout_toLeftOf="@+id/btn_continue"
        android:layout_toStartOf="@+id/btn_continue"
        android:layout_marginRight="35dp"
        android:layout_marginEnd="35dp" />

    <Button
        android:id="@+id/btn_continue"
        android:layout_width="100dp"
        android:layout_height="30dp"
        android:background="@android:drawable/btn_default"
        android:clickable="true"
        android:text="Fortsette"
        android:textColor="#5DBCD2"
        android:textSize="12dp"
        android:layout_below="@+id/txt_dia"
        android:layout_alignRight="@+id/txt_dia"
        android:layout_alignEnd="@+id/txt_dia" />

</RelativeLayout>